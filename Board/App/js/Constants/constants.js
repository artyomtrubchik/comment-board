import keyMirror from 'keymirror';

export default keyMirror({
    COMMENTS_LOADED: null,
    LOAD_MORE_COMMENTS: null,
    APPLY_FILTER: null,
    APPLY_ONLY_POSITIVE_FILTER: null,
    APPLY_ONLY_NEGATIVE_FILTER: null,
    APPLY_EVERY_FILTER: null,
});
